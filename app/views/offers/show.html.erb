<h1>Offers#show</h1>
<p><%= @offer.title %></p>
<p><%= @offer.description %></p>

<%= link_to "Back to the home page", offers_path %>

<%# create button to edit the offer %>
<div class="mt-2">
  <%= link_to "Edit Offer", edit_offer_path(@offer), class: "btn btn-primary" %>
</div>
